# Config local/personal de Claude Code (no se comparte, cada dev tiene la suya)
.claude/settings.local.json

# Residuo de un comando mal ejecutado en PowerShell (ej. `comando > nul`)
nul

# Capturas/logs del MCP de Playwright (pruebas manuales en navegador)
.playwright-mcp/
